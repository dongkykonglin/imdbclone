/** @type {import('next').NextConfig} */
///const nextConfig = { image: { domains: ["image.tmdb.org"] } };

//module.exports = nextConfig;
const nextConfig = {
  //   reactStrictMode: false,
  //   swcMinify: true,
  images: {
    domains: ["image.tmdb.org"],
  },
};

module.exports = nextConfig;
